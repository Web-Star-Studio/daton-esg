"""Extraction subsystem.

Reads project documents (indexed in Pinecone) and produces structured
suggestions for materiality (`Project.material_topics`, `Project.sdg_goals`)
and indicators (`Project.indicator_values`).
"""
